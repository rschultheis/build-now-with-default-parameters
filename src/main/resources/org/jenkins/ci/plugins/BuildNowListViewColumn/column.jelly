

<?jelly escape-by-default="true"?>
<j:jelly xmlns:j="jelly:core">
  <td>
    <j:if test="${job.hasPermission(job.BUILD)}">

    <img class='build-now-with-defaults'
         data-job-name="${job.name}"
         src="${imagesURL}/${subIconSize}/next.png"
         alt="${%BuildNowListViewColumn.Configure.Title}" />

    </j:if>
  </td>
</j:jelly>
